import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const configPath = path.join(__dirname, 'database/config/config.js');
const modelsPath = path.join(__dirname, 'database/models');
const seedersPath = path.join(__dirname, 'database/seeders');
const migrationsPath = path.join(__dirname, 'database/migrations');

export {configPath as config, modelsPath as 'models-path', seedersPath as 'seeders-path', migrationsPath as 'migrations-path'};